language: node_js
node_js:
  - 6
cache:
  directories:
    - client/node_modules
    - server/node_modules
before_install:
  - npm install -g @angular/cli
install:
  - cd client
  - ng build -prod
  - cd ../server
  - npm install
script:
  - cp -rf ../client/dist ./client
  - zip rcr-national-archive.zip